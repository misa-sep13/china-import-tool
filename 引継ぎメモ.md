# 引継ぎメモ

家など別のパソコンで続きをやる場合、このファイルをClaudeに読ませれば経緯がわかります。
（このファイル自体はGitHubにpushされているので、`git pull`すれば家のPCでも見えます）

---

## 2026-06-19 のやり取り（最新）

### Render を有料プランにアップグレード
- スピンダウンが無くなり、サーバー・キャッシュが常時生存するようになった。

### Amazon 発注まわりの改善
- **全在庫一覧（StockPage）に検索窓を追加**（SKU・商品名・色/サイズで絞り込み）。後で位置を「再計算ボタンの横」に移動。
- **「発注」ボタンを追加**（楽天と同じ行ごと発注）。`POST /api/orders/order` で押した商品だけを発注済リストに記録（Excel生成なし）。Amazon発注推奨リスト＋全在庫一覧の両方に設置。
- **「発注推奨リストが開いた瞬間に全件チェック→Excel押すと全件が発注済になる」不具合を修正**（自動全選択を廃止。チェックは手動）。
- 発注済リストのボタンは最終的に**「リストから外す」1つに統一**（納品も誤発注もこれで外す。在庫はSP-APIから自動取得なので在庫加算は不要）。
- 未選択行が薄グレー(opacity)で見にくかったのを修正（選択行は薄青背景に）。

### Amazon 在庫一覧に「非表示」ボタンを追加
- セラセンでSKUをリネーム（例：`y48_glay-m`→`y48_glay-m_2`）すると、FBA取込は新SKUを足すだけで旧SKUを消さないため二重に残る。
- 在庫一覧の各行に「非表示」ボタン（`DELETE /api/products/{id}` でソフト削除 `is_active=False`、後で復元可）を追加し、その場で旧SKUを片付けられるようにした。
- 旧SKU `y48_glay-m / glay-s / pink-m / pink-s`（id 22,23,24,25）はこの日ソフト削除済み。

### Render メモリ上限超過の対策
- 原因：バックグラウンドジョブ結果(`_jobs`)が掃除されず溜まるリーク。キャッシュ温めcronが加速していた。
- `_prune_jobs`（20分超・上限30件で削除）を追加。SP-APIキャッシュ温めcronを30分→**毎時1回**に削減。キャッシュTTL 60→70分。

### SP-API 売上取得の高速化（試行→リバート）
- 5期間別取得を「90日を日別1回取得」に変える最適化を入れたが、**レート制限で日販が0だらけ**になり発注計算が壊れたため**リバート済み**（commit 368a044 をrevert）。再挑戦するならレート制限対策（リトライ強化・並列数抑制）が必須。

### 楽天 発注管理を実稼働させた（この日のメイン）
- 症状：在庫は取れるが日販・提案発注数が出ない。原因は売上同期(`/rms/sync`)が受注60日分を1リクエストで処理して**502タイムアウト**→何も保存されず売上0のまま、だった。
- 対応：
  1. **バックグラウンドジョブ化**：`POST /api/rakuten/rms/sync/start` ＋ `GET /api/rakuten/rms/sync/status/{job_id}`。設定ページの「今すぐ販売データ同期」ボタンもポーリング式に更新。
  2. **受注取得を並列化＋1回100件取得**（`rakuten_rms.py`：全注文番号を先に集めて getOrder を BATCH_SIZE=100・6並列）。10分近く→約6分に短縮。
  3. 集計のタイムゾーン比較バグ（naive vs aware）を `.date()` 比較に修正。
  4. 取得日数は **60日**（元の設計値。直近30日=recent / 31〜60日前=prev で成長率も算出）。
  5. **毎日 JST 3:00 に自動同期**する cron 追加（`.github/workflows/rakuten-sales-sync.yml`）。
- 結果：328 SKU取得・249商品に売上反映・日販/成長率/提案発注数が出る状態に。**稼働中**。
- SKU照合キー：商品の `rakuten_sku_id`（=楽天variantId。色名が入っている）または `sku`（商品管理番号）で受注データと突き合わせる。これが効いて209件マッチした。

### この日時点の本番状態
- GitHub Actions ワークフロー：`backup-db.yml`（毎日DBバックアップ）/ `deploy.yml`（フロント自動デプロイ）/ `refresh-fees.yml` / `warm-cache.yml`（SP-API毎時ウォーム）/ `rakuten-sales-sync.yml`（楽天売上毎日同期）。
- 未コミットのまま残っているファイル：`gen_y77_csv.py`, `y77_products.csv`（リポジトリ直下）。

---

## 過去のメモ（2026-06-16）

## 今日やったこと
- y127〜y131系 19商品を `is_component=True` にして内部SKU扱いに変更（完了）
- Supabaseのバックアップ/PITRを実際にログインして確認 → Freeプランのため復元手段なしと確定
- `Book1.csv`（タオタロウ発注データ）を正としてy系80商品の仕入原価(cost_jpy)を修正（完了）
- y83を y76/y77 と同じ「単品＋選べるバリエーション」のアコーディオン表示に統一（完了。set_componentsは元から正しかったので単品側のis_componentをFalseに戻すだけで対応）
- Supabase Freeにバックアップ機能がないため、GitHub Actionsで毎日DBを丸ごとJSONダンプしてリポジトリにコミットする仕組みを新設（`.github/workflows/backup-db.yml`、直近30日分を保持）
- y76の単品5色（black/white/darkglay/navy/lightglay）の仕入URL・単価(3.5元)・仕様（中国語）を1688.comの情報で更新（完了）
- スキーマの `spec`（システム連携用SKU番号）と `supplier_spec`（仕様（中国語）)を取り違えていたミスを修正済み

## まだ手付かず・要判断
1. y131の以下6SKUは `Book1.csv` に該当データがなく、cost_jpy/buy_url/price/selling_price/notes/stock が未復旧のまま：
   - y131_holder_blue / holder_pink / holder_purple
   - y131_strap_blue / strap_pink / strap_purple
2. y77ファミリーは `Book1.csv` 内で同じSKUに対し矛盾する単価（204 vs 96など）があり、今回の80件修正から除外。どちらを正とするか要確認。
3. id 249,250,251,258,259（s02-1-banana/grape/lemontea/peach、仕入先：マレフィオーレ）— 以前から壊れていた商品。対応未着手。
4. 未コミットのまま残っているファイル：`gen_y77_csv.py`, `y77_products.csv`（リポジトリ直下）→ コミットするか削除するか未確定。

## 重要な作業ルール（必ず守る）
- 商品データを更新する時は **PUT で全フィールド送信** が必要な場面があるため、必ず `GET /api/rakuten/products` で現在値を取得 → 変更したいフィールドだけ書き換え → PUT、の順で行う（id/created_at/sales_updated_at は送らない）。過去にこれを怠って大量データ消失した事故があったため。
- `spec` = システム連携用SKU番号 / `supplier_spec` = 仕様（中国語） ← 紛らわしいので要注意。
- 複数商品への一括変更は、ユーザーの指示が単数形でも対象を勝手に広げない。曖昧なら確認する。

## 本番環境
- Backend: https://china-import-tool.onrender.com （Render, FastAPI）
- DB: Supabase Postgres（Freeプラン、project ref: xxgecnucixnkitnjzupk）
- リポジトリ: https://github.com/misa-sep13/china-import-tool
