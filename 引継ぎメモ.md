# 引継ぎメモ

家など別のパソコンで続きをやる場合、このファイルをClaudeに読ませれば経緯がわかります。
（このファイル自体はGitHubにpushされているので、`git pull`すれば家のPCでも見えます）

---

## 2026-08-17 のやり取り（最新）

### 外注さん用アクセス制御（簡易ログイン）を導入・完成

- 経緯: 外注さんに本URLをそのまま渡すのは危険なので、専用ログイン＋更新履歴の可視化＋日次バックアップでの復元可能性、を要望されて実装。
- 認証方式: JWTライブラリ等は使わず、stdlibのhmac/hashlibだけで署名トークンを自作（`backend/app/core/auth.py`）。ロールは `owner` / `contractor` / `service`（GitHub Actions用）。
- `AUTH_OWNER_PASSWORD` が未設定の間は今までどおり無認証で動く（fail-open設計。デプロイしただけでロックアウトされない）。
- 全APIを `main.py` のミドルウェアで一括保護。設定(`/settings`系)への書き込みのみ外注さんに403、読み取りは他画面が使うので通す。RMSのAPIキーだけレスポンスからマスク。
- `activity_logs` テーブルを新設し、商品マスタ登録/更新/削除・在庫の手動変更・発注削除を記録。トップページのロゴクリックで更新履歴パネル（自分/外注さん/自動処理のバッジ付き）に表示。
- **オーナーが「パスワードを毎回打ちたくない」との要望** → 初回ログイン後に `?auth=トークン` 付きURLを表示、ブックマークすれば次回から無入力。トークンに発行時点のパスワードの指紋(pwfp)を埋め込み、パスワード変更で即座に全トークン無効化できるようにした（TTLは365日だが実質の失効手段はパスワード変更）。
- 設定手順（Renderの環境変数4つ + GitHub Actions secretsに1つ）は依頼者に案内済み、依頼者側で設定完了・動作確認済み。

### 副次的に発見・修正したバグ（すべて修正・デプロイ済み）

1. **就労支援荷受けExcel取込後、最新日タブでなく前回選んでいた古いタブが開いたままになる**バグ → 取込直後に選択をリセットして常に最新タブを開くように修正。
2. **仕入管理のインボイス照合漏れ**: 急ぎの分だけ別の配送依頼として提出し、実際は他の便の箱に同梱される場合（追跡番号欄に「(相手の配送依頼No)に梱包」というメモが入る運用）、従来はインボイス番号と追跡番号が完全一致する配送依頼しか見ていなかったため同梱分の商品が照合対象から漏れていた。同梱された配送依頼も追いかけるように修正。
3. **「配送依頼」タブが認証必須化後に「ログインが必要です」エラーで一切使えなくなっていた**：このページの通信だけが認証トークンを自動付与する共通クライアントを使わず生のaxiosを直接使っていたのが原因（上記1の認証導入による副作用）。共通クライアントに統一して修正。
4. **仕入管理の検算NGバグ（重要・根深い）**: 送料を箱の計費重量で按分する機能で、按分方法は成功したと表示されるのに合計が常に0元になり保存できない問題。原因はJSONの仕様上オブジェクトキーが必ず文字列になることで、サーバーが作る箱番号(int)キーの辞書がフロント経由で往復すると文字列キーに化け、中身リスト側の箱番号(int)と型不一致でルックアップが常に外れていた。**このバグは今回の1件に限らず、箱単位で送料配分するインボイス全般に影響していた可能性が高い**（過去に保存した原価が本来より低く出ていた可能性）。両側を文字列キーに揃えて修正。

### 現在の本番状態

- `origin/main` = `a5bad33`。上記すべてデプロイ済み・動作確認済み。
- 認証は依頼者側で有効化済み（オーナー・外注さんとも実際にログインしてブックマーク運用に入っている）。

### まだ手付かず・要判断の事項

- **検算NGバグ（上記4）による過去の原価への影響範囲は未調査**。バグ修正前に保存された箱重量按分ありのインボイスは、送料が過小に配分された誤った原価で保存されている可能性がある。過去のインボイス保存履歴を洗い出して再計算し直すべきか、依頼者の判断待ち。
- `activity_logs` によるログ記録は全操作を網羅していない（在庫の書き換え・マスタ登録/更新/削除・発注削除のみ）という制約を依頼者に明示済み・了承済み。
- 就労支援画面の検索ボックスに `ime-mode: active` を付けたが、これは現行Chrome/Edgeでは効果のない非推奨CSSプロパティである点は依頼者に開示済み（使用ブラウザ次第では実質ノーオペ）。

---

## 2026-07-31 〜 08-04 のやり取り

### ⚠️ このセッションは china-import-tool ではなく **Amazon Seller Scout** の作業

- リポジトリ: `C:\Users\misa1\amazon-seller-scout` → https://github.com/misa-sep13/amazon-seller-scout （Private）
- 本番: https://amazon-seller-scout-je48.onrender.com （Render / Starter / Virginia）
- DB: **TiDB Cloud（Manus時代と同じものを継続使用）**。データ移行は一切していない
- 内容: **リサーチツールをManusから自前環境へ完全移行**。Manus依存はコードから消滅済み

---

### やったこと（全5フェーズ + 追加改修）

#### フェーズ1: Manus依存の撤去（`6f9b495`）
- `vite-plugin-manus-runtime` / Manus analytics(umami) / 未使用テンプレ6件 / `ManusDialog.tsx` / `Map.tsx` を削除
- **Keepa連携を全削除**。`uploadSellerSearchResults` は拡張から呼ばれる設計だったが**メッセージ送信元がどこにも無い完全な死にコード**だった（APIキーも未設定で常時エラー）。ユーザー確認済みで削除
- `cross-env` 追加でWindowsでも `pnpm dev` が動くように
- **`index.html` が 367.83kB → 0.87kB**（Manusランタイムが全ページに367KB注入していた）

#### フェーズ2: Manus OAuth → 自前のメール+パスワード認証（`b52775d` `041cbce`）
- `users.passwordHash` 追加（migration 0017）、`_core/password.ts`（node:crypto の scrypt、新規依存なし）
- `auth.login` / `pages/Login.tsx` / `scripts/set-password.ts` を新設
- `_core/oauth.ts` と `types/manusTypes.ts` は削除
- **`users.id` / `openId` は一切変更していない**ため、セラー・商品・フォルダの紐付けは完全維持

#### フェーズ3: 定期更新を Manus Heartbeat → 外部cron（`f461a67` `f7aa05e`）
- `/api/scheduled/updateSellers` を `X-Cron-Secret` ヘッダーの共有シークレット認証に変更（timingSafeEqual、`CRON_SECRET` 未設定なら503）
- **既存バグも修正**: このエンドポイントはESMファイル内で `require()` を使っており**呼ばれた時点で必ず落ちる**状態だった。エラー応答にstack traceを含めていたのも修正
- `_core/heartbeat.ts` `_core/notification.ts` 削除

#### フェーズ4: Chrome拡張のManus依存撤去（`55336d9`）
- manifest を `*.onrender.com` + localhost に差し替え（Renderは `*.onrender.com` なのでURL未確定でも先に対応できた）
- `content.js` / `content_seller_search.js` に **`https://amseller-app.manus.space` がフォールバックで埋め込まれていた**（設定を忘れると旧Manusに送信する事故になる）。撤去し、未設定ならエラーで中断するように

#### フェーズ5: Renderデプロイ（`c9a32b0` `cad9499`）
- `render.yaml` + `DEPLOY.md` 追加
- PORT明示時に空きポートを探して勝手にずらす挙動を廃止（Renderで "no open ports detected" になる）

#### 追加改修（08/04）: セラー更新の実用化（`6b932a2` `255ae8a` `562d762`）★重要
- **収集ページ数 3→4**（上位商品が4ページ目に現れるセラーがいるため）
- **「自動で次のセラーに進まない」の根本原因を特定・修正**:
  セラーリスト全体をBase64でURLに詰めて持ち回る設計だったため、件数に比例してURLが肥大していた。
  `10件→約6,200文字 / 30件→約12,000文字 / 100件→約33,000文字 / 352件→約110,000文字`
  **「30件だとAmazonがNGを出す」はAmazon側の制限ではなくURL長超過が実態だった。**
  → セラーリストを `chrome.storage.local` に預け、URLには `current_index` だけを載せる方式に変更。**URL長は件数によらず約200文字で一定**。
  保存・取得・クリアの仕組み（`background.js` の saveSellerList/getSellerList/clearSellerList、`content_webapp.js` の中継）は**既に実装済みで未使用だった**ので繋ぎ込んだ。
  batch_index/total_batches/all_sellers の二重バッチ管理は不要になり削除（約100行→約40行）
- **ボタン整理**: 「本日以外をすべて更新」新設（1クリックで対象抽出から更新まで、件数上限なし）/ 「本日以外を30件選択」廃止 / 「本日以外を10件選択」は手動用に残置
- **収集しても日付が更新されない問題を修正**（原因3つ）:
  1. `saveProducts` が `lastCollectedAt` を一切更新していなかった（書いていたのは定期実行の経路だけ）
  2. `isNew: 0` だけを書いていたため、既に0のセラーは値が変わらずMySQLが行を更新せず `updatedAt`(ON UPDATE CURRENT_TIMESTAMP) も据え置きだった
  3. 画面表示は `lastCollectedAt`、「本日以外」判定は `updatedAt` と**見ている項目が食い違っていた**
  → 結果、日付が 2026/2/9 のまま動かず同じセラーを何度も更新し続ける状態だった。判定を `lastCollectedAt` に統一（`isNotCollectedToday`）

#### その他
- Webアプリにファビコン追加（`cd73030`）、拡張アイコンを同デザインに刷新（`a144328`）
- `scripts/backup-db.ts`（DBダンプ）/ `dedupe-sellers.ts` / `test-password.ts` / `get-api-key.ts` / `add-password-column.ts` を追加

---

### 現在の本番状態

- `origin/main` = **`562d762`**（GitHub反映済み）
- Renderは `255ae8a` までデプロイ確認済み。**`562d762`（日付更新の修正）のデプロイ結果は未確認** → 要確認
- **ログイン可能**: ゆな（`misa16@rj8.so-net.ne.jp` / **id=8100161**）、ちば（`miyauty3838@gmail.com` / id=330156）
- 拡張機能 **v4.1.0**、作業フォルダは `chrome-extension`（英語名）、配布zipは `セラースカウト用拡張機能-v4.1.0.zip`
- **DBバックアップ取得済み**: `backups/backup-20260731-101343.sql`（37.9MB / 商品82,274・セラー3,093・コレクション8,708）

### DB上の重要な注意点

- **`misa16@rj8.so-net.ne.jp` が2アカウント存在**: `id=1`(ゆな/admin/1740行/348店) と **`id=8100161`(現役/1098行/352店)**。
  ユーザー確認済みで「id=1 → 8100161 に移行済み、id=1は削除して構わない」。
  **同一メール重複は `set-password.ts`(--id必須) と `auth.login`(全件照合) の両方で対処済み。片方だけ直すとログイン不能になる**
- **`pnpm db:push` は使わないこと**。`__drizzle_migrations` は17行(0000〜0016)で、0017は `add-password-column.ts` の生ALTERで適用したため未記録。重複適用で失敗する
- **収集履歴8,708件のうち309件が存在しないセラーを指している**（参照先115種、Manus時代のコピー作業の残骸）。商品82,274件の孤立はゼロ

### この環境特有のハマりどころ（重要）

- **Runボタン経由だと標準入力が繋がらない** → 対話プロンプトのスクリプトは永久に待って「何も起きない」ように見える。入力は `password.txt` や環境変数で渡す
- **子プロセスのstdoutがユーザーに見えないことがある** → 結果は `*> ログファイル` に落として読む方が確実
- **コードブロックはユーザーが編集できない** → 「ここを書き換えて」形式は渡さない。メモ帳を開いて入力させる
- **PowerShellの実行ポリシーが `pnpm.ps1` を拒否** → `node node_modules\tsx\dist\cli.mjs <script>` が最も確実
- **Bashからは日本語パスが見えない**（MSYS2とWindowsの正規化不一致）。日本語フォルダ名にしたら**3回消失事故が発生**し、1度はGit上からもファイルが失われた（過去コミットから復元）。**拡張機能フォルダは英語名で固定すること**
- Renderの Build Command に **`corepack enable` を付けてはいけない**（`/usr/bin/pnpm` が読み取り専用で EROFS エラー）
- Renderの環境変数に **`NODE_ENV` を置いてはいけない**（ビルド時にも効いてdevDependenciesが入らず失敗）

### まだ手付かず・要判断（Seller Scout）

1. **`562d762` のRenderデプロイ確認**。日付更新の修正が本番に入っているか
2. **拡張機能を外注さんへ配布**（`セラースカウト用拡張機能-v4.1.0.zip`）。Webアプリのデプロイ完了後に送ること（順序を誤ると新拡張×旧アプリで動かない）
3. **`id=1` の扱い**。削除すると**未処理セラー候補945件**と id=1 にしか無い5店が失われる（バックアップには残る）
4. **重複セラーの整理**。`scripts/dedupe-sellers.ts`（既定は確認のみ、`--apply` で実行）。バックアップで検証済み: 3,093行→955行、収集履歴8,708件は完全維持
5. **TiDBの契約者がManusの可能性が高い**。Manus解約でクラスタごと消える恐れ。バックアップは取得済みだが、自分名義のDBへ移す判断が必要
6. **`JWT_SECRET` と TiDBパスワードのローテーション**。zipに平文で入っていた（`.project-config.json`）
7. ちばさんのパスワード `miyauty38ss` は一度チャットに平文で登場したので変更推奨
8. 定期更新のcron未設定（`CRON_SECRET` は設定済みだが、Render Cron Job は未作成）

---

## 2026-07-28 のやり取り

### やったこと

#### 1. Renderデプロイ失敗の修正（`ca7f7c6`）
- Codex並行マージのコミット `d5efbc2` が `backend/app/main.py` にマージコンフリクトマーカーを残したままpushされ、さらにUTF-8 BOMも付いてデプロイが失敗していた。
- コンフリクト31箇所を正しいUTF-8側で解決し、BOMを除去。以降デプロイ成功。
- **7/22と同じ事故が再発している。Codexとの並行作業後は `python -c "import ast; ast.parse(open('backend/app/main.py',encoding='utf-8').read())"` で必ず構文チェックすること。**

#### 2. 楽天在庫・損益一覧に「売価同期(RMS)」ボタンを追加（`e1c2d83`）
- 商品マスタページにしか無かった `POST /rakuten/rms/sync-prices` のボタンを在庫・損益ページにも設置。

#### 3. 日販計算の是正（`310b1df`）
- y135_black（出したばかりで売上1件）の発注推奨が66個になっていた。
- `_daily_avg_from_table` の分母が「データがある日数」だったため、1件の売上が日販1.0と評価されていた。
- 分母を「ウィンドウ日数(7日/30日) − 在庫切れ日数」に変更。1件なら 1÷7=0.14 になる。

#### 4. RMS在庫反映の失敗を記録・可視化・補正できるようにした（`9247330` / `f30bb8a` / `9d9162f`）★このセッションのメイン
- **発端**: 配送依頼の入荷で「RMS反映: ok 19 / fail 4」が出たが、どのSKUが失敗したかどこにも残らなかった。
- **原因**: `429 Rate Limit`。10件同時PUT・リトライ最大4秒では足りず弾かれていた（404やSKU設定ミスではない）。
- 新テーブル `rms_push_failures` を追加し、失敗SKUを永続化（HTTPステータス・理由・発生元つき）。
- `push_inventory_and_record()` ラッパーを新設し、`push_inventory_to_rms` の全呼び出しを置き換え。
  **在庫編集・一括保存・メーカー入荷の3箇所はバックグラウンド実行で失敗が完全に握り潰されていた。**
- 成功したSKUは過去の未解決failureを自動でresolvedにする。
- 追加API: `GET /rakuten/rms/push-failures` / `POST /rakuten/rms/push-failures/retry`（補正push・現在のDB在庫を送る） / `POST /rakuten/rms/push-stock`（DBを変えずRMSへ再送。skus指定可）
- 429対策: 同時PUT 10→5、バッチ間1秒、リトライ4→6回、`Retry-After`尊重＋ジッタ付き指数バックオフ。
- UI: 配送依頼の入荷結果に失敗SKU表＋補正pushボタン。在庫・損益ページに未解決failureの常時警告バナー＋「🔄 在庫を楽天へ再送」ボタン。

#### 5. 配送依頼の入荷まわりのバグ修正（`30c209a` / `456e78d`）
- **発注済2が繰り上がらないバグ**: `SessionLocal`が`autoflush=False`のため、消化で立てた`is_deleted=True`が未flushのまま繰り上げ判定に入り「消化済みの発注済1がまだ残っている」と誤判定していた。判定前に`db.flush()`。
- **重複紐づけ行が無言で捨てられる問題**: 同一商品に複数行が紐づくと2行目以降が黙って破棄され在庫が入らなかった。理由つきで画面に表示するようにした。
- **未反映行の再取り込み機能**: `shipment_order_items.is_reflected` を追加し、`POST /shipment-orders/{id}/receive-remaining` で未反映かつ照合済みの行だけを取り込めるようにした。
  既存の入荷済みは全てTRUEで埋めるマイグレーション付き（二重加算しない）。
- **入荷済みでも紐づけを変更可能に**。反映済みの行を付け替えた場合は元SKUの在庫を自動で戻し、取消を在庫反映履歴に残す。
- UI: 配送依頼一覧に「在庫未反映N件」バッジ、詳細に警告・再取込ボタン・「在庫反映」列。

#### 6. 就労支援ページの修正（`f618fcf` / `fd36171` / `38b9967`）
- **備考が消える問題**: 実際は保存できていた。保存成功時にドラフトを破棄してから一覧をinvalidateしており、荷受け一覧は全820行の画像(base64)を含むため再取得に数秒かかり、その間だけ入力欄が空に巻き戻っていた。PATCHの戻り値でキャッシュを直接差し替えるように変更（在庫側の商品名欄も同様に修正）。
- **荷受けタブが日付でない問題**: シート名が日付として読めない場合（`物流面单`・`ﾚｯｸﾞｿｰﾌﾟ`）にシート名がそのままタブ名になっていた。取り込んだ日(created_at)をM/D表示にした。日付シートのタブは不変。
- 就労支援在庫の検索欄に `imeMode: 'active'` を付けて日本語入力で始まるようにした。

### 直した本番データ（このセッションで実施）
- **RMS未反映だった4SKU**（`y95_black` 87 / `445` 22 / `y112_white` 152 / `y85_pink` 15）を補正pushして反映。全て429が原因。
- **ペット用ハンモックL の紐づけ間違いを是正**: 配送依頼#8(VIP6003083180)でL の3行が全て `y96_l_leaf` に紐づいており、重複判定で2行が破棄され在庫30個が消えていた。
  - `y96_l_ster` 在庫 0→15、`y96_l_graycheck` 在庫 0→15（RMSにも反映済み）
  - 発注 id=84 / id=82 を消化済みにし、id=131(5) / id=104(6) / id=133(15) を発注済2→1へ繰り上げ
  - 配送依頼#8のitem 111→y96_l_ster、item 113→y96_l_graycheck に紐づけ修正
  - ※ y96_m_graycheck（M-グレーチェック）の紐づけは元から正しかった
- 就労支援の福祉在庫の重複（y76_darkgray id=353 / y76_lightgray id=352）を削除。

### 現在の本番状態
- `origin/main` = `fd36171`。Renderバックエンド・GH Actionsフロントともデプロイ済み。
- RMS未解決failureは 0件（`GET /rakuten/rms/push-failures` で確認可能）。
- 配送依頼の未反映行: #8=7件 / #7=1件 / #5=8件 / #4=7件（すべて未照合の行。再取込可=0なので誤って在庫が増えることはない）。
  - #8の未反映7件の内訳は「梱包用ビニール袋3行（販売品ではないので対応不要）」＋「ins大容量简约纯色背包 リュック4色 計10個（商品マスタ未登録）」。
- 未コミットのまま残っているファイル: `gen_y77_csv.py`, `y77_products.csv`（リポジトリ直下）

### まだ手付かず・要判断
1. **リュック（`https://detail.1688.com/offer/919688986279.html`）10個が商品マスタ未登録で在庫に入っていない。** 登録して配送依頼#8の詳細から「未反映の行を取り込む」を押せば在庫に入る。販売予定が無いなら放置で可。
2. **商品分析(Analytics)ページのSP-API取得が12分かかる。** 完走はするが遅すぎる。SKU数×レート制限が原因。要最適化。
3. **就労支援の荷受け一覧APIが全行の画像(base64)を返しており1回で数MB。** 画像を別APIに分離すればページが軽くなり、Supabaseの帯域も節約できる。
4. Supabase無料プラン帯域超過の猶予期間（2026-08-13まで）— 対策未定
5. y131の6SKU・y77ファミリーの単価矛盾・id 249-251,258-259の壊れた商品 — 引き続き未対応
6. Codexとの並行作業でmain.pyが2回壊れている。マージ後は必ず構文チェックすること。

---

## 2026-07-22 のやり取り

### やったこと

#### 1. GitHub Actions 売上同期のタイムアウト修正
- Daily Rakuten Sales Sync (#34) が `httpx.ReadTimeout` で失敗していた。336 SKU × 4388日分を1リクエストで送信していたのが原因。
- `scripts/sync_rakuten_sales.py` を50 SKUずつバッチ送信（timeout=300s）に変更。
- `backend/app/api/routes/rakuten.py` の `apply_daily_sales` をN+1クエリからPostgreSQL `ON CONFLICT DO UPDATE` バルクupsert（500行チャンク）に変更。

#### 2. Render デプロイ失敗の修正
- Codex並行マージで `backend/app/main.py` の日本語文字がバイト化けし、Python 3.14のSyntaxErrorでRenderデプロイ失敗。
- `ec49829` から `main.py` を復元し、SEOインポート/ルーターを手動追加。

#### 3. SEOキーワード208件を一括登録
- ユーザーが提供したGoogleスプレッドシートの内容（キーワード＋SKUペア）をAPIに一括登録（`scripts/register_seo_keywords.py`使用）。

#### 4. SeoPageのAPIパス重複修正
- `SeoPage.jsx` で `${API}/api/seo/...` としていたが `API` はすでに `/api` を含むため `/api/api/seo/...` になっていた。10箇所を `${API}/seo/...` に修正。

#### 5. SEO順位マトリクス表示＋自動日次チェック（メイン）
- **バックエンド**:
  - `GET /seo/rankings/matrix?days=30` — 全アクティブキーワード × 日付のマトリクスデータ（SKU順、各セルはその日の最良順位）
  - `POST /seo/rankings/bulk` — GH Actionsからの一括順位登録エンドポイント
- **フロントエンド** (`SeoPage.jsx` 全面書き換え):
  - スプシ風マトリクステーブル（行=キーワード、列=日付、セル=順位）
  - SKU列でグルーピング、色分け（緑:1-10位、黄:11-30位、赤:31位+、灰:圏外）
  - SKU・キーワード列はsticky（横スクロール対応）
  - 期間切替（7/14/30/90日間）
  - キーワード管理はモーダルに移動
- **GitHub Actions** (`seo-check.yml` + `scripts/check_seo_rankings.py`):
  - 毎日 JST 0:00（UTC 15:00）に自動実行 + 手動実行可
  - GH Actionsランナー上で楽天検索を直接スクレイピング（Renderの512MBメモリ制限を回避）
  - 2秒間隔でキーワード処理、1.5秒間隔でページ遷移、結果を100件バッチでRenderに送信
  - 208キーワード × 最大5ページ → 推定50分以内（timeout 60分）

### 現在の本番状態
- コミット `c88f502` が `origin/main` にpush済み → Render自動デプロイ + GH Actionsフロントデプロイが走る
- SEO順位データはまだ入っていない。初回は GH Actions「Daily SEO Ranking Check」を手動実行するか、今夜JST 0時の自動実行を待つ
- GitHub Actions ワークフロー一覧: `backup-db.yml` / `deploy.yml` / `refresh-fees.yml` / `warm-cache.yml` / `rakuten-sales-sync.yml` / **`seo-check.yml`（NEW）**
- 未コミットのまま残っているファイル: `gen_y77_csv.py`, `y77_products.csv`（リポジトリ直下）

### まだ手付かず・要判断
1. SEO順位チェックの初回実行がまだ（手動 or 自動で実行後にマトリクス表示を確認する）
2. Supabase無料プラン帯域超過の猶予期間（2026-08-13まで）— 対策未定
3. y131の6SKU・y77ファミリーの単価矛盾・id 249-251,258-259の壊れた商品 — 引き続き未対応
4. `BACKEND_URL` secretがGH Actionsに設定済みか要確認（既存の`rakuten-sales-sync.yml`で使っているので問題ないはず）

---

## 2026-06-19 のやり取り

### Render を有料プランにアップグレード
- スピンダウンが無くなり、サーバー・キャッシュが常時生存するようになった。

### Amazon 発注まわりの改善
- **全在庫一覧（StockPage）に検索窓を追加**（SKU・商品名・色/サイズで絞り込み）。後で位置を「再計算ボタンの横」に移動。
- **「発注」ボタンを追加**（楽天と同じ行ごと発注）。`POST /api/orders/order` で押した商品だけを発注済リストに記録（Excel生成なし）。Amazon発注推奨リスト＋全在庫一覧の両方に設置。
- **「発注推奨リストが開いた瞬間に全件チェック→Excel押すと全件が発注済になる」不具合を修正**（自動全選択を廃止。チェックは手動）。
- 発注済リストのボタンは最終的に**「リストから外す」1つに統一**（納品も誤発注もこれで外す。在庫はSP-APIから自動取得なので在庫加算は不要）。
- 未選択行が薄グレー(opacity)で見にくかったのを修正（選択行は薄青背景に）。

### Amazon 在庫一覧に「非表示」ボタンを追加
- セラセンでSKUをリネーム（例：`y48_glay-m`→`y48_glay-m_2`）すると、FBA取込は新SKUを足すだけで旧SKUを消さないため二重に残る。
- 在庫一覧の各行に「非表示」ボタン（`DELETE /api/products/{id}` でソフト削除 `is_active=False`、後で復元可）を追加し、その場で旧SKUを片付けられるようにした。
- 旧SKU `y48_glay-m / glay-s / pink-m / pink-s`（id 22,23,24,25）はこの日ソフト削除済み。

### Render メモリ上限超過の対策
- 原因：バックグラウンドジョブ結果(`_jobs`)が掃除されず溜まるリーク。キャッシュ温めcronが加速していた。
- `_prune_jobs`（20分超・上限30件で削除）を追加。SP-APIキャッシュ温めcronを30分→**毎時1回**に削減。キャッシュTTL 60→70分。

### SP-API 売上取得の高速化（試行→リバート）
- 5期間別取得を「90日を日別1回取得」に変える最適化を入れたが、**レート制限で日販が0だらけ**になり発注計算が壊れたため**リバート済み**（commit 368a044 をrevert）。再挑戦するならレート制限対策（リトライ強化・並列数抑制）が必須。

### 楽天 発注管理を実稼働させた（この日のメイン）
- 症状：在庫は取れるが日販・提案発注数が出ない。原因は売上同期(`/rms/sync`)が受注60日分を1リクエストで処理して**502タイムアウト**→何も保存されず売上0のまま、だった。
- 対応：
  1. **バックグラウンドジョブ化**：`POST /api/rakuten/rms/sync/start` ＋ `GET /api/rakuten/rms/sync/status/{job_id}`。設定ページの「今すぐ販売データ同期」ボタンもポーリング式に更新。
  2. **受注取得を並列化＋1回100件取得**（`rakuten_rms.py`：全注文番号を先に集めて getOrder を BATCH_SIZE=100・6並列）。10分近く→約6分に短縮。
  3. 集計のタイムゾーン比較バグ（naive vs aware）を `.date()` 比較に修正。
  4. 取得日数は **60日**（元の設計値。直近30日=recent / 31〜60日前=prev で成長率も算出）。
  5. **毎日 JST 3:00 に自動同期**する cron 追加（`.github/workflows/rakuten-sales-sync.yml`）。
- 結果：328 SKU取得・249商品に売上反映・日販/成長率/提案発注数が出る状態に。**稼働中**。
- SKU照合キー：商品の `rakuten_sku_id`（=楽天variantId。色名が入っている）または `sku`（商品管理番号）で受注データと突き合わせる。これが効いて209件マッチした。

### この日時点の本番状態
- GitHub Actions ワークフロー：`backup-db.yml`（毎日DBバックアップ）/ `deploy.yml`（フロント自動デプロイ）/ `refresh-fees.yml` / `warm-cache.yml`（SP-API毎時ウォーム）/ `rakuten-sales-sync.yml`（楽天売上毎日同期）。
- 未コミットのまま残っているファイル：`gen_y77_csv.py`, `y77_products.csv`（リポジトリ直下）。

---

## 過去のメモ（2026-06-16）

## 今日やったこと
- y127〜y131系 19商品を `is_component=True` にして内部SKU扱いに変更（完了）
- Supabaseのバックアップ/PITRを実際にログインして確認 → Freeプランのため復元手段なしと確定
- `Book1.csv`（タオタロウ発注データ）を正としてy系80商品の仕入原価(cost_jpy)を修正（完了）
- y83を y76/y77 と同じ「単品＋選べるバリエーション」のアコーディオン表示に統一（完了。set_componentsは元から正しかったので単品側のis_componentをFalseに戻すだけで対応）
- Supabase Freeにバックアップ機能がないため、GitHub Actionsで毎日DBを丸ごとJSONダンプしてリポジトリにコミットする仕組みを新設（`.github/workflows/backup-db.yml`、直近30日分を保持）
- y76の単品5色（black/white/darkglay/navy/lightglay）の仕入URL・単価(3.5元)・仕様（中国語）を1688.comの情報で更新（完了）
- スキーマの `spec`（システム連携用SKU番号）と `supplier_spec`（仕様（中国語）)を取り違えていたミスを修正済み

## まだ手付かず・要判断
1. y131の以下6SKUは `Book1.csv` に該当データがなく、cost_jpy/buy_url/price/selling_price/notes/stock が未復旧のまま：
   - y131_holder_blue / holder_pink / holder_purple
   - y131_strap_blue / strap_pink / strap_purple
2. y77ファミリーは `Book1.csv` 内で同じSKUに対し矛盾する単価（204 vs 96など）があり、今回の80件修正から除外。どちらを正とするか要確認。
3. id 249,250,251,258,259（s02-1-banana/grape/lemontea/peach、仕入先：マレフィオーレ）— 以前から壊れていた商品。対応未着手。
4. 未コミットのまま残っているファイル：`gen_y77_csv.py`, `y77_products.csv`（リポジトリ直下）→ コミットするか削除するか未確定。

## 重要な作業ルール（必ず守る）
- 商品データを更新する時は **PUT で全フィールド送信** が必要な場面があるため、必ず `GET /api/rakuten/products` で現在値を取得 → 変更したいフィールドだけ書き換え → PUT、の順で行う（id/created_at/sales_updated_at は送らない）。過去にこれを怠って大量データ消失した事故があったため。
- `spec` = システム連携用SKU番号 / `supplier_spec` = 仕様（中国語） ← 紛らわしいので要注意。
- 複数商品への一括変更は、ユーザーの指示が単数形でも対象を勝手に広げない。曖昧なら確認する。

## 本番環境
- Backend: https://china-import-tool.onrender.com （Render, FastAPI）
- DB: Supabase Postgres（Freeプラン、project ref: xxgecnucixnkitnjzupk）
- リポジトリ: https://github.com/misa-sep13/china-import-tool
