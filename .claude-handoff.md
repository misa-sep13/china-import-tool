# 引継ぎメモ

## 今日やったこと（2026-06-20〜21）

1. **s03商品登録**
   - ライオン チェックアップ ルートケアα 90g 1本（SKU: s03）を商品マスタに登録
   - 仕入価格631円、仕入れ先マレフィオーレ
   - s03-3をs03×3本のセット商品として設定

2. **商品マスタページ（/rakuten/products）の422クラッシュ根治**
   - `response_model` 依存を除去・`model_validate` 使用に変更
   - `backend/app/api/routes/rakuten.py`

3. **発注管理の複数バグ修正**
   - 発注推奨リストに閾値外（在庫日数40日超）の商品が出る問題を修正
   - 全商品から発注タブをSKU順にソート
   - RMS在庫取得で `normal-inventory`（末尾スペース付き等）の無効SKUが原因でAPI全体400エラーになる問題を修正（正規表現フィルター）
   - 更新ボタンが無反応になる問題を修正（`isLoading`→`isFetching`・`staleTime: 0`）
   - y69花がらコースター各色が発注推奨に単品で出る問題を修正（`y69_flower`のbuy_urlを設定）
   - y104・446が発注推奨リストに出ない問題を修正（`singles`の条件をbuy_urlベースに変更）

4. **発注管理UI統合（今セッションの最終作業）**
   - 「発注推奨リスト」と「全商品から発注」タブを「発注管理」1タブに統合
   - データソース：`/rakuten/orders/all-products` を常時使用
   - 上部＋右下フローティングに「全商品 ⇔ 発注推奨のみ」トグルボタン
   - `frontend/src/pages/RakutenOrderPage.jsx` を書き換え済み・push済み

## 次回やること

1. **統合後UIの動作確認**（GitHub Actionsでデプロイ後）
   - フロント: https://misa-sep13.github.io/china-import-tool/
   - 「全商品」「発注推奨のみ」トグルが切り替わるか確認
   - 右下フローティングボタンが表示されるか確認
   - 在庫数・日販・在庫日数が正しく表示されるか確認

2. **RMSキー再発行**（優先度：高）
   - 今セッションでAPIレスポンスにrms_service_secret / rms_license_keyが混入して露出
   - 楽天RMS管理画面でキーを再発行
   - 再発行後はSupabaseの `rakuten_settings` テーブルを更新

3. **在庫連動切り替えの継続**（前回セッションから未完了）
   - 同期ログで計算値と別ツールの値が一致したら `push_inventory_to_rms` を有効化
   - `main.py` の `_sync_rakuten_stock` 内の `rms_would_update` を実際の反映に変更
   - 切り替え前に「在庫取得(RMS)」ボタンでDBをRMSに合わせること

4. y125・y134系のbuy_url・仕様・単価を手動で再入力（前回から継続）

## 作業中のファイル

- `frontend/src/pages/RakutenOrderPage.jsx` - 統合UI実装完了・push済み
- `backend/app/api/routes/rakuten.py` - 発注推奨ロジック修正・push済み
- `backend/migrate_add_show_in_orders.py` - 未コミット（マイグレーションスクリプト、不要なら削除可）

## メモ・注意点

- リポジトリ: https://github.com/misa-sep13/china-import-tool
- 本番API: https://china-import-tool.onrender.com/api
- 本番フロント: https://misa-sep13.github.io/china-import-tool/
- ローカル起動: `cd C:\Users\misa\Projects\china-import-tool\backend; python -m uvicorn app.main:app --port 8000`
- **git push はユーザーが手動実行**（Claude Code の auto-mode でブロックされるため）
- **複数のClaudeが同時に本番DBを操作しないこと**
- `normal-inventory` というSKUが商品マスタに存在する（is_component=True・buy_url=null）→ RMS API送信対象から除外済み
- セット商品の `set_components` は JSON 形式で `[{"sku": "...", "qty": N, "buy_url": "...", "price": 元値}]`
- `is_component=True` は内部管理SKU（発注リスト非表示）、`is_component=False` は通常商品
- 発注推奨ロジック：`/rakuten/orders/all-products` が全商品を返し、各商品の `needs_order` フラグでフィルタリング
- 在庫連動の現状フロー：楽天注文→1分後に受注取得→DBの在庫を減算→RMSへの反映はまだログのみ
