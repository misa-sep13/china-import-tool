# 引継ぎメモ

## 今日やったこと

### 発注管理の表示ロジック修正（長期対応）
- y76系セット組（y76_b-b等25件）・y77系（81件）・y83系（36件）を `is_component=False` に設定（セット組は発注しない）
- y76単色品（y76_black等5件）を `is_component=True` に設定（単品として発注推奨リストに出す）
- y133本体（cat/egg/ribon/penguin×pink/blue 8件）を `is_component=True` に変更→その後 `is_component=False` に戻す（一覧表示のため）
- y133付属品（strap_pink/blue/film_comp 3件）を `is_component=True`（内部管理SKU）に設定
- 推奨リストの除外ロジックを整理：`is_component=True` + `buy_url`あり（シンプル化）
- 全商品タブの除外ロジック：`is_component=False` + `buy_url`あり + `set_components`なし

### 会社のClaudeによるデータ破損と復旧
- y133系全件のbuy_url・set_componentsが空になる事故が発生（会社のClaude側の操作ミス）
- このセッションの会話履歴に残っていたJSONデータから y133系11件を完全復元
- y125・y134系のbuy_urlは復元不可（会話履歴になかった）

### データ登録
- y134系4色（clear/blue/pink/purple）+ y134_film_comp：URL・仕様・単価13元・film_comp付属のset_components登録
- y91系4色（blue/clear/pink/black）：URL・仕様（蓝色/白色/粉色/黑色12粒）・単価4.1元・備考「3.8元交渉済」登録

### 安全対策
- グローバルCLAUDE.mdに本番DB破壊的変更防止ルールを追記
- 楽天商品マスタ407件のバックアップJSONをGitにコミット（backups/rakuten_products_20260616_215450.json）
- 会社のClaude側でPUTエンドポイントが部分更新（exclude_unset=True）に修正済み（全フィールド上書き事故の根本対策）
- GitHub Actionsによる日次自動バックアップが会社のClaude側で実装済み（.github/workflows/backup-db.yml）

### 発注管理UI改善（最後の作業）
- 全商品タブに `is_component=False` の縛りを追加（y47_coffee等の内部SKUが出ていた問題を修正）
- 推奨リストのレスポンスに `set_components` を追加
- 推奨リストの本体行の下に付属品をバリエーション表示（└ 仕様 単価元 URL）

## 途中の作業
- 発注管理の付属品バリエーション表示：コミット済み（ced2a23）・pushは未実施（ユーザーに依頼中）
- デプロイ後の動作確認がまだ：y133_cat_pinkの下にストラップ・フィルムが表示されるか、y47_coffee等が全商品タブから消えているか

## 未解決の問題
- y125・y134系のbuy_urlが空のまま（会社のClaudeの操作ミスで消えた、復元不可）→ 手動で再入力が必要
- Supabaseのパスワードがこのセッションの会話に複数回露出→ ローテーション推奨（後回しにしている）
- `backend/migrate_add_show_in_orders.py` が未コミットのまま残っている（今回は使わなかったので削除してよい）

## 次回やること
1. pushの確認（`cd C:\Users\misa\Projects\china-import-tool; git push origin main`）
2. デプロイ後の動作確認（推奨リストの付属品表示・全商品タブの内部SKU除外）
3. y125・y134系のbuy_url・仕様・単価を手動で再入力
4. Supabaseパスワードのローテーション
5. あざみ（就労支援）在庫管理の実装（次の大きな機能）

## 現在の状態
- 本番API: https://china-import-tool.onrender.com/api
- 本番フロント: https://misa-sep13.github.io/china-import-tool/
- GitHubリポジトリ: https://github.com/misa-sep13/china-import-tool

## 重要メモ
- **is_componentの整理済みルール**：
  - `is_component=True` = 推奨リストに出す発注単位SKU（y76_black、y133_strap_pink等）
  - `is_component=False` = 通常商品・セット組（商品マスタ一覧に表示される）
  - ※ y133本体はis_component=False（一覧に出る）かつbuy_urlあり・set_componentsあり → 全商品タブ対象
- **全商品タブの除外条件**：is_component=False + buy_urlあり + set_componentsなし
- **推奨リストの対象**：is_component=True + buy_urlあり
- **PUTエンドポイントは部分更新になった**（exclude_unset=True）→ 送らなかったフィールドは上書きされない（安全）
- 複数のClaudeが同時に本番DBを操作しないこと（事故の主因）
- Supabaseパスワードが会話に露出済み → できるだけ早くローテーションを
