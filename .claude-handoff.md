# 引継ぎメモ（2026-06-28 更新）

## 現在の状態

- **Stage 4 稼働中** — 全面切替完了
- **RMS_PUSH_ENABLED=true**（2026-06-27 16:42 デプロイ）
- **既存.NETツール: 停止済み**（再開しない）
- 最新コミット: `d09cefd`（main）
- リポジトリ: https://github.com/misa-sep13/china-import-tool （Private）
- 本番API: https://china-import-tool.onrender.com/api
- 本番フロント: https://misa-sep13.github.io/china-import-tool/
- ローカル起動(backend): `cd C:\Users\misa\Projects\china-import-tool\backend; python -m uvicorn app.main:app --port 8000`
- デプロイ: `git push` でフロント(GitHub Actions)・バックエンド(Render)が両方自動デプロイ
- 未追跡: `backend/migrate_add_show_in_orders.py`（不要なら削除可）

## 異常時ロールバック

1. Render環境変数: RMS_PUSH_ENABLED=false に変更
2. Render再起動
3. 既存.NETツール再開

## 完了済みの作業

### push検証済みSKU
- 243: 239×1受注により本来484→482となるべきところ、RMS反映が確認できなかったため、debug-push-executeで243=482 / 239=241 / 241=160へ補正済み
- 244: diff=0確認
- 258: push成功（155→152）、その後受注で149まで減少、auto push正常
- 259: push成功（922→919）、diff=0維持
- y115_10: debug-push-executeでRMS 237→232に補正済み、diff=0

### DB補正済みSKU
- y76: 5構成品（black=557, darkglay=219, lightglay=260, white=252, navy=157）
- y83: 5構成品（beige=26, darkgray=37, gray=42, lightgray=55, pink=10）
- y77: 8構成品補正済み
- 258=152, 259=919, y115_10=237→232, s02-1-banana=474
- s02-1-orange=35, s02-1-mint=236

### 修正したバグ
- **is_component push/pullバグ**（a123374）: `is_component=true`かつ`rakuten_item_url`ありのSKU（16件）がauto push/pullから除外されていた → `is_component and not rakuten_item_url`に条件変更（main.py 2箇所 + rakuten.py 2箇所）
- **同一SKUセット計算バグ**（dd0efc4）: y76_b-b等で同一構成品が複数回出現する場合にqtyを合算してから計算するよう修正
- **受注検索窓拡大**（812bc49）: 2分→15分に拡大、processed_ordersで二重減算防止
- **フロントエンド表示**（353383a）: 仕入原価ヘッダー(元)→(円)
- **auto pushログ記録**（e53677a）: push結果（対象SKU・数量・HTTPステータス・成功/失敗件数・エラー内容）をsync-logsに記録。受注sync時・キャンセル戻し時の両方で動作確認済み
- **y134本番事故対応・purchase_components分離**（ae6db64, 1cf7f49）: y134_film_compがset_componentsに入っていたため全色SKUが0にpushされた事故。set_components（在庫連動用）とpurchase_components（発注用・在庫連動しない）を分離
- **全件監査・purchase_components移行（27件）**: y134事故を受け、set_componentsに「発注用付属品」が入っている全SKUを洗い出し、purchase_componentsへ移行。対象: y133系8件、y134系4件、y48系4件、y60系2件、y91系4件、y124、y15、y34、y69_flower、y47。全27件でset_components=空、purchase_components=あり、DB/RMS diff=0、component起点preview sets=[]を確認済み。set_componentsに残っている176件（240/242/y76/y77/y83等）は正当な在庫連動セットとして変更なし
- **inventory_events テーブル追加**（9c0eba4, 57660bc）: 在庫変動イベントをDBに永続化。sync_logs（メモリdeque）と並行してDB保存。event_type: order_sold / cancel_restore / manual_adjust / debug_push。stock_before/stock_after、push結果、注文番号をJSON保存。GET /api/inventory-events で最新100件取得可能。event_timeはJST naive保存（UTC変換防止済み）
- **就労支援在庫管理**（235ee44〜d09cefd）: welfare_inventory_items テーブル、作業指示明細、残量直接修正、スプレッドシート移行API

### 新商品登録
s11, s02-orange系, s02-mint系（8 SKU）、cost_jpy更新済み

## 監視項目（5点）

1. inventory-events にpush失敗がないこと（GET /api/inventory-events で確認）
2. y115_10 previewがdiff=0を維持すること
3. is_component=true + rakuten_item_urlありのSKU（16件）受注時にcomponent自体もRMS反映されること
4. 既存ツールは再開しない
5. HTTP 429（レート制限）の再発監視 — ID 20: y76系10件同時push→y76_black/y76_b-dが429（補正済み）。ID 86: y83_be-gr受注push→4件429（現時点残diffなし、補正不要）。再発時は要対応

### is_component=true + rakuten_item_url ありのSKU一覧（16件）
y115_10, y127_10, y127_6, y128, y129_black, y129_gray, y129_navy,
y130_beige, y130_brown, y130_gray, y130_ivory,
y131_dinosaur_blue, y131_rabbit_pink, y131_unicorn_purple,
y48_bag, y60_bag

## 未解決・保留

- **243のauto push未反映の原因**: 17:40の受注で243を482に減算→auto push実行されたはずだがRMS=484のまま。pushログ追加済みなので次回発生時に原因追跡可能
- **RMSキー再発行**: backupsがGitHubに入り漏洩扱い（有効期限2026-07-22）
- **y76 SKU名不一致**: RakutenProduct="glay"、products="gray"。現在影響なし、テーブル統合時に要対応
- **inventory_events の既存6件（id 1〜6）**: event_timeが9時間ずれ（UTC保存）。id 7以降は修正済みだがデプロイタイミングによりid 7〜15も一部ずれている可能性あり。実害なければそのまま
- SS販売数の発注計算への加算
- y125系のbuy_url・仕様・単価の手動再入力

## 補正push履歴（2026-06-28）

- **第1回（ID 20起因）**: y76_black起点 ok=9, y76_darkglay起点 ok=9, y77_black起点 ok=17（y76_b-d: 218→249 含む）
- **第2回（全起点健診）**: 12秒間隔で順次実行
  - y76_white ok=9, y76_lightglay ok=9, y76_navy ok=9
  - y77_pastel ok=17, y77_vivid ok=17, y77_brown ok=17, y77_pink ok=17, y77_purple ok=17, y77_blue ok=17, y77_green ok=17
- 最終確認: y76 全5起点・y77 全9起点すべて nonzero_diff_count=0、全件HTTP 204、fail=0

## 重要ルール
- **git push はユーザーが手動実行**（PowerShell形式で提示）
- **本番DB変更前に対象レコードの全フィールドを取得・保持**
- **execute前に必ずpreviewでpush_valueを確認**
- **set_components = 売れる数を決める部品**（受注時に構成品在庫を増減、RMS在庫再計算に使用）
- **purchase_components = 仕入れ時に必要な部品**（RMS在庫連動しない、TAO太郎Excel展開とCSVのみ）
- **main.pyの在庫連動ロジックはpurchase_componentsを絶対に参照しない**

## スケジューラ構成（main.py）
- 起動時: `_seed_processed_orders()` → `_pull_rms_stock()`
- 1分ごと: `_sync_rakuten_stock()` → `_pull_rms_stock()`
- 30分ごと: `_check_delayed_cancellations()`
- 60分ごと: `_sync_rakuten_sales()`
