# 引継ぎメモ

## 今日やったこと（2026-06-26）

### 1. コードレビュー指摘4件を全修正【完了】

前セッション（2026-06-25）で在庫連動の根本問題を修正したが、コードレビューでP1×3、P2×1の問題が見つかったので全件修正。

#### P1-1: `_processed_orders`のDB永続化 + 初回seed
- **問題**: 処理済み注文がインメモリdict/dequeで管理されており、Render再起動/デプロイ時に全消失 → 直近注文が二重減算される
- **修正**: `processed_orders`テーブルを新設（`backend/app/models/processed_order.py`）。DB永続化で再起動に耐える。
- **初回seed**: デプロイ直後にテーブルが空なら、過去7日分の注文番号をRMSから取得し「在庫を動かさずに」seedする。旧プロセスで処理済みの注文を新プロセスが二重減算しない。
- **cleanup**: active注文は7日間保持（遅延キャンセル追跡用）、cancelled注文は30日超で削除。

#### P1-2: 遅延キャンセル検出 + バックフィル
- **問題**: `fetch_recent_orders`のsearchOrderは直近2分のウィンドウしか検索しない。数時間後・翌日のキャンセルは検出できず、在庫が戻らない
- **修正**: `_check_delayed_cancellations()`関数を新設。30分ごとにDB内のactive注文をRMS getOrderで一括再確認し、orderProgress=900に変わった注文の在庫を自動で戻す。
- **バックフィル**: 初回seedで過去7日分のactive注文がDBに入るため、デプロイ前に処理済みだった注文のキャンセルも追跡可能。

#### P1-3: `_sync_rakuten_stock`にpush呼び出し追加
- **問題**: 受注減算でDB在庫を更新しても`push_inventory_to_rms`を呼んでいない。RMS_PUSH_ENABLED=trueにしても自動syncの結果がRMSに反映されない
- **修正**: `_sync_rakuten_stock`の末尾に変更されたSKU（単品＋セット）のpush呼び出しを追加。`_check_delayed_cancellations`にも同様のpush追加。RMS_PUSH_ENABLED=falseの間はpush_inventory_to_rms内部でスキップされるので安全。

#### P2: searchOrderのページネーション
- **問題**: `requestPage: 1`固定で100件までしか取得しない。セール時に2分間で100件超の注文があると一部が漏れる
- **修正**: ページループ追加（100件未満が返るか10ページまで繰り返し）

### 2. 在庫連動の根本修正（2026-06-25、前セッション分）

**根本原因**: `_pull_rms_stock`と`_sync_rakuten_stock`が毎分同時に走り、受注減算した単品在庫をpullが楽天値で上書きしていた。

**修正内容** (コミット `5beb401`):
- セットの構成品になっている単品SKU（21件）をpull上書きからスキップ
- 受注検知→単品在庫減算＋セット再計算を実装
- キャンセル（orderProgress=900）の状態遷移追跡・在庫戻し
- 汚染値（243=536/244=264/251=52/259=937）は手動修正済み

### 3. debugエンドポイント（2026-06-25）
- `GET /rakuten/rms/debug-inventory?manage_number=s08` — bulk-get生レスポンス表示
- `GET /rakuten/rms/debug-pull-missing` — pull取りこぼしSKU特定

### 4. ドキュメント作成（2026-06-25）
- `docs/在庫売価連動_設計.md` — 全体設計・段階的解禁計画
- `docs/在庫ズレ問題の説明.md` — 問題の分かりやすい説明

---

## 段階的解禁の進捗

| 段階 | 内容 | 状態 |
|---|---|---|
| 0 | 取りこぼし11件の正体特定 | 完了（楽天に存在しないvariantId） |
| 1 | 受注/キャンセル減算の実装 | 完了・本番稼働中（P1修正含む） |
| 2 | 1〜2 SKUだけ実push検証 | 次 |
| 3 | 既存ツール実数と突合 | -- |
| 4 | 全面解禁(RMS_PUSH_ENABLED=true)＋既存ツール停止 | -- |

## 次回やること（優先度順）

1. **段階2: pushの段階的検証** — 特定SKUだけpushできるデバッグ用エンドポイント作成、china-import-toolのDB値と楽天値を突合
2. **既存ツール（24時間PC）との並行稼働の方針決定** — 最終的にchina-import-toolに一本化して既存ツールを停止
3. **RMSキー再発行** — backupsにGitHub入り→漏洩扱い（有効期限2026-07-22）
4. y76/y83の在庫ズレ調査（複数セットが無く逆算不能）
5. SS販売数の発注計算への加算（super_sale_qty連動）
6. y125・y134系のbuy_url・仕様・単価の手動再入力

## 現在の状態

- リポジトリ: https://github.com/misa-sep13/china-import-tool （Private）
- 本番API: https://china-import-tool.onrender.com/api
- 本番フロント: https://misa-sep13.github.io/china-import-tool/
- pushフラグ: `RMS_PUSH_ENABLED=false`（楽天への書き込みは全面停止中）
- ローカル起動(backend): `cd C:\Users\misa\Projects\china-import-tool\backend; python -m uvicorn app.main:app --port 8000`
- デプロイ: `git push` でフロント(GitHub Actions)・バックエンド(Render)が両方自動デプロイ
- 未追跡: `backend/migrate_add_show_in_orders.py`（不要なら削除可）

## 重要メモ
- **git push はユーザーが手動実行**（PowerShell形式で提示）
- **本番DBはInvoke-RestMethodで生レスポンスを取る**（WebFetchは一部SKU欠落）
- **本番DB変更前に対象レコードの全フィールドを取得・保持**（PUT全件上書き事故防止）
- 比較は必ず**既存ツール（24時間PC）の実数**を正とする
- RMS APIキー漏洩 → 再発行必要（有効期限2026-07-22）

## スケジューラループ構成（main.py）
- 起動時: `_seed_processed_orders()`（テーブル空なら過去7日分を在庫操作なしでseed）→ `_pull_rms_stock()`
- 1分ごと: `_sync_rakuten_stock()` → `_pull_rms_stock()`
- 30分ごと: `_check_delayed_cancellations()`
- 60分ごと: `_sync_rakuten_sales()`
