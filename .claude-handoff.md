# 引継ぎメモ（2026-07-27 更新）

## 現在の状態

- **Stage 4 稼働中** — 全面切替完了
- **RMS_PUSH_ENABLED=true**（2026-06-27 16:42 デプロイ）
- **既存.NETツール: 停止済み**（再開しない）
- リポジトリ: https://github.com/misa-sep13/china-import-tool （Private）
- 本番API: https://china-import-tool.onrender.com/api
- 本番フロント: https://misa-sep13.github.io/china-import-tool/
- ローカル起動(backend): `cd C:\Users\misa\Projects\china-import-tool\backend; python -m uvicorn app.main:app --port 8000`
- ローカル起動(frontend): `cd C:\Users\misa\Projects\china-import-tool\frontend; npm run dev`
- デプロイ: `git push` でフロント(GitHub Actions)・バックエンド(Render)が両方自動デプロイ

## 今日やったこと（2026-07-26〜27）

### 就労支援在庫アップロード確認
- send-order-list (16).xls の25件が全て登録済みであることを確認

### FBA納品プラン機能の新規実装（デプロイ済み）
- バックエンド: `/api/fba-plan/start`, `/status/{job_id}`, `/export-excel` の3エンドポイント
- フロントエンド: FbaPlanPage.jsx（フィルタタブ all/air/sea/hold/need、ソート、Excel出力、除外チェック）
- 設定画面にFBAリードタイム関連7項目追加
- OrderSettingsモデルに7カラム追加（lt_order_to_warehouse, lt_shipping_request, lt_sea_to_fba, lt_air_to_fba, free_storage_days, air_threshold_days, hold_daily_threshold）
- 関連ファイル:
  - `backend/app/api/routes/fba_plan.py` — プラン計算・Excel出力API
  - `frontend/src/pages/FbaPlanPage.jsx` — UI
  - `backend/app/models/settings.py` — 7カラム追加
  - `frontend/src/pages/SettingsPage.jsx` — 設定UI追加

### FBA納品プランの考え方
- **発注管理（75日）と納品プランは別物**: 発注管理=仕入先に何個発注するか、納品プラン=TAO太郎倉庫からFBAにいつ・どう送るか
- 目標在庫日数83日 = 無料保管90日 − 発注〜倉庫着7日
- 保留判定: 日販0.1未満は送らない（TAO太郎で返品/廃棄の選択肢を残す）
- 航空便判定: パイプライン残日数 ≤ 18日なら船便では間に合わないので航空便

## 途中の作業（次回ここから再開）

なし

## 未解決の問題

- **RMSキー有効期限切れ**: 2026-07-22で切れている可能性あり（要確認・再発行）
- **main.pyのコメント文字化け**: 一部mojibakeが残っている（動作に影響なし）
- **y76 SKU名不一致**: RakutenProduct="glay"、products="gray"。現在影響なし
- **Amazon Ads API**: 審査中（2026-06-07申請）

## 次回やること

1. FBA納品プランの本番動作確認
2. RMSキーの有効期限確認・再発行
3. Amazon向け関税率別計算の実装（楽天と同様のアプローチ）
4. SP-API Fulfillment Inbound API連携（Phase 3、API権限解決後）
5. 損益計算・ダッシュボード機能の実装

## Amazon Ads 広告管理（Phase 0+1+2 完了）

- **Phase 0**: API疎通・DB保存 — SP v3 campaigns/adGroups/keywords/targets + 3レポート取得、6テーブルへupsert
- **Phase 1**: 読み取り専用ダッシュボード — キャンペーン/KWパフォーマンス/検索語句の3タブ、フィルタ・ソート・ACOS色分け
- **Phase 2**: 提案一覧 + 結果パネル — KW昇格・入札調整・予算調整の提案表示
- **注意**: 入札変更、KW追加、停止、除外KW登録などのAPI更新系操作は未実装（意図的）
- Profile ID: `879360668189955` (JP)、Base URL: `https://advertising-api-fe.amazon.com`

## 重要ルール
- **git push はユーザーが手動実行**（PowerShell形式で提示）
- **本番DB変更前に対象レコードの全フィールドを取得・保持**
- **execute前に必ずpreviewでpush_valueを確認**
- **set_components = 売れる数を決める部品**（受注時に構成品在庫を増減、RMS在庫再計算に使用）
- **purchase_components = 仕入れ時に必要な部品**（RMS在庫連動しない、TAO太郎Excel展開とCSVのみ）
- **main.pyの在庫連動ロジックはpurchase_componentsを絶対に参照しない**

## 異常時ロールバック

1. Render環境変数: RMS_PUSH_ENABLED=false に変更
2. Render再起動
3. 既存.NETツール再開

## 監視項目（5点）

1. inventory-events にpush失敗がないこと（GET /api/inventory-events で確認）
2. y115_10 previewがdiff=0を維持すること
3. is_component=true + rakuten_item_urlありのSKU（16件）受注時にcomponent自体もRMS反映されること
4. 既存ツールは再開しない
5. HTTP 429（レート制限）の再発監視

## スケジューラ構成（main.py）
- 起動時: `_seed_processed_orders()` → `_pull_rms_stock()`
- 1分ごと: `_sync_rakuten_stock()` → `_pull_rms_stock()`
- 30分ごと: `_check_delayed_cancellations()`
- 60分ごと: `_sync_rakuten_sales()`
