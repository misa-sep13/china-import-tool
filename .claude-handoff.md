# 引継ぎメモ（2026-08-03 更新）

## 現在の状態

- **Stage 4 稼働中** — 全面切替完了
- **RMS_PUSH_ENABLED=true**（2026-06-27 16:42 デプロイ）
- **既存.NETツール: 停止済み**（再開しない）
- リポジトリ: https://github.com/misa-sep13/china-import-tool （Private）
- 本番API: https://china-import-tool.onrender.com/api
- 本番フロント: https://misa-sep13.github.io/china-import-tool/
- ローカル起動(backend): `cd C:\Users\misa\Projects\china-import-tool\backend; python -m uvicorn app.main:app --port 8000`
- ローカル起動(frontend): `cd C:\Users\misa\Projects\china-import-tool\frontend; npm run dev`
- デプロイ: `git push` でフロント(GitHub Actions)・バックエンド(Render)が両方自動デプロイ

## 今日やったこと（2026-08-02〜03）

### Amazon仕入管理（インボイス）を楽天と同等に作り直し

- **判明した重大な不具合**: `save_invoice` が `customs_duty` 等6列を `Invoice` に渡すのに
  モデルに列が無く `TypeError`。本番のインボイス登録が0件だったのはこれが原因とみられる
- **輸入税が原価に入っていなかった**（送料しか按分していない）。楽天は税込み
- 共通サービス `backend/app/services/invoice_calc.py` を新設。
  `parse_permit_columns` / `match_items_to_columns` / `calc_tariff_tax` を
  rakuten.py から切り出し、ルータのスキーマ依存を外して両者から使う
  （楽天側は薄いラッパにして委譲。**計算結果が変わらないことをスナップショット比較で検証済み**）
- `invoices.py` を楽天と同じロジックに: 申告欄ごとの税率計算、SKU空欄スキップ、
  `set_size` で割った原価。計算は `_build_cost_rows` に集約し calculate と save で結果がずれないようにした
- **`products.cost_jpy` を新設**。従来は円原価を `price`（元単価）に上書きしており、
  発注管理の「単価(元)」が円に化ける状態だった
- `InvoicePage.jsx` を作り直し。**タブ順を「登録 → 一覧」に変更**（登録が主作業のため）

### 在庫がセラーセントラルとずれる問題を修正

- `reservedQuantity` の合計を「作業中」扱いしていたのが誤り。中身で意味が違う:
  - `pendingCustomerOrder` = 注文が入り出荷準備中
  - `pendingTransshipment` = FC間移動中（**すでにFBA在庫**）
  - `fcProcessing` = 庫内作業中（セラセンの「入出荷作業中」）
  - 移動中を「まだ売れない」扱いにしていたため、在庫があるのに発注推奨が出ていた
- **在庫キャッシュを5分TTLに分離**（`_CACHE_TTL_INVENTORY`）。売上（220req・約7分）と
  同じ4時間TTLだったため、納品や販売が最大4時間反映されなかった
- **sessionStorageに保存時刻を持たせた**。従来はフィールドの有無しか見ておらず、
  タブを開いている限り何時間でも古い在庫を表示し続けていた（10分で作り直す）

### 発注済みの消し込み（FBA納品実績との突合）

- Fulfillment Inbound API連携を実装。`GET /orders/delivery-candidates` と
  `POST /orders/mark-shipped` を追加し、発注済みリストタブから消し込めるようにした
- **v0 getShipments は ShipmentStatusList を複数まとめて渡すと1件しか返さない**。
  1つずつ指定して結合すると過去180日で22件・全SKUの実発送数が取れる
- 発注日より前の納品は別ロットなので充当しない。`DELIVERY_MATCH_SINCE=2026-07-27` 以降のみ対象
  （それ以前は発注データと納品数が食い違っており、遡ると誤って消し込むため）
- 同日・同数量の納品は作り直しとみなして1件に寄せる
- 発注数と納品数が一致する組み合わせを優先して割り当てる（単純な先入先出だと
  7/21発注70個に対する7/28納品70個が7/02発注に吸われてしまう）

### その他

- 発注管理に**成長率の列**を追加（直近7日・15日平均 vs 90日。発注数補正と同じ基準）
- 発注管理に**「作業中」列**を追加。右下のフローティングボタンを削除
- 発注管理のテーブルを楽天と同じ `sticky-table` にして見出し行を固定
- 納品済み(shipped)の発注を発注済み集計から除外（`_not_shipped()`）
- FBA納品プランに**タオタロウ直送の手順**を折りたたみ（details/summary）で追加
- y134_purple_2 の FNSKU 誤登録を修正（X001D1YWHH → X001D8JKF9。実在庫10個が0と表示されていた）
- a01/a04 を納品済みにして発注済みリストから削除

## 前回やったこと（2026-08-01）

### Amazon発注管理を楽天と同じ「推奨／全商品」切替に改修

- **原因**: `orders.py` が `if calc.qty == 0: continue` で発注数0の商品を配列から捨てていたため、そもそも全商品を出せなかった
- バックエンド（`backend/app/api/routes/orders.py`）:
  - 上記 `continue` を削除（非同期ジョブ `_run_preview_job` と同期版 `preview_orders` の2箇所）
  - 各商品に `needs_order: calc.qty > 0` を追加（楽天の `needs_order` に相当）
- フロントエンド（`frontend/src/pages/OrderPage.jsx`）:
  - 楽天と同じトグルボタン（上部＋右下フローティング）・SKU/商品名の検索欄を追加。初期表示は「発注推奨のみ」
  - 推奨行をオレンジ（`#fff7ed`）で色分け
  - **選択管理を配列インデックス（`_idx`）→ `product_id` ベースに変更**。従来のままだとフィルタで表示行が変わった瞬間にチェックが別商品にずれる
  - **Excel出力・件数・合計を `items`（表示中のみ）→ `allItems` 基準に修正**。チェック後に検索や切替で隠れた商品が警告なくExcelから抜け落ちる問題があった
- 実データ確認: 全47件返却、推奨12件／非推奨35件。改修前に表示されていた12件と一致

### 発注数の計算方法（AmazonとRakutenは別ロジック・今回は計算式自体は未変更）

**Amazon**（`backend/app/services/calc.py`）
```
日販   = 7日×0.05 + 15日×0.15 + 30日×0.25 + 60日×0.25 + 90日×0.30
成長補正 = 直近(7日,15日平均)÷90日 → 1.3以上:1.0 / 0.7以下:0.5 / 中間:1.0
目標在庫 = 日販 × 成長補正 × (リードタイム75日 + セール上乗せ日数)
発注数  = ceil((目標在庫 − 総在庫) ÷ セット入数)  ※ピース換算10個未満は0に切り捨て
総在庫  = FBA販売可能 + 納品中 + 梱包中 + extra_stock + 発注済
```

**楽天**（`backend/app/services/rakuten_calc.py`）
```
日販   = 7日×0.4 + 30日×0.6（7日が30日の1.5倍超ならキャップ・在庫切れ日は除外）
発注数  = 予測販売(target_days30日) + リード期間販売(20日) + 安全在庫(10%) − 総在庫
needs_order = 総在庫 ≤ 日販 × threshold_days(40日)
```

- 推奨判定はAmazon側を「発注数 > 0」とした（楽天の「在庫日数が閾値以下」とは基準が異なる）
- Amazonの `CalcSettings.threshold_days` は現在75日で `lead_days` と同値のため、楽天式に揃えてもほぼ同じ結果になる見込み

### 補足
- ローカル起動用に `.claude/launch.json` を作成（backend:8000 / frontend:5173）。不要なら削除可
- SP-APIのプレビュー取得は実測で**7分以上**かかった（UI表示は「2〜3分」）。商品数増加で伸びている可能性あり

## 前回やったこと（2026-07-29）

### FBA納品プランページ改修
- サイドバーの「納品プラン」を発注管理の直下に移動
- OrderHistoryモデルに`status`（ordered/arrived/shipped）、`arrived_at`、`taotaro_order_id`カラム追加
- バックエンドAPI追加:
  - `GET /api/fba-plan/orders` — ASIN/FNSKUがある発注済み商品の一覧（楽天商品を除外）
  - `POST /api/fba-plan/orders/{id}/status` — ステータス手動変更
  - `POST /api/fba-plan/import-taotaro` — タオタロウExcelアップロード → buy_urlの1688 IDで照合 → ステータスをarrivedに更新
- フロントエンド（FbaPlanPage.jsx）を全面リニューアル:
  - タオタロウExcel取込ボタン（照合結果・未照合リスト表示）
  - 発注済みリストをステータス別に色分け表示（発注済=黄、到着済=緑）
  - 手動ステータス変更ボタン（「到着済みにする」「配送依頼済み」）
  - 到着済み商品がある場合のみ、SP-API在庫取得→FBA納品プランセクション表示
- main.pyのマージコンフリクト（30件以上）を全て解消

### タオタロウExcelの構造確認
- ドメインは yiwutaro.com
- URL: `https://www.yiwutaro.com/member/order-list?products=[]&state=4`（state=4が入庫済み）
- Excelは21カラム: 発注時間、オーダー番号、商品名、色、サイズ、商品URL、単価、数量、ASIN、FNSKU、状態、到着時間など
- 拡張子は.xlsだが実際はxlsx形式（PKヘッダー）→ openpyxlで読み込み可能

## 途中の作業（次回ここから再開）

- **最終pushの本番確認が未実施**（コミット 8448387 まで push 済み、デプロイ待ちの状態で終了）
  - 確認すべきもの:
    1. 在庫キャッシュ5分化＋sessionStorage 10分で、**手動更新なしに在庫が最新になるか**
       → 期待値: y84_black 販売可能68/作業中2/発注済0、y84_blue 販売可能78/作業中2/発注済0
       （2026-08-03 02:00時点。セラーセントラルの手持ち在庫と一致することを本番APIで確認済み）
    2. FBA納品プランの「📋 タオタロウFBA直送の流れ」が折りたたみで出るか
    3. **Amazon仕入管理が実ファイルで通るか**（下記の未検証事項）
- **Amazon仕入管理は実インボイスでの検証が未実施**。
  自作のテストxlsx/pdfでは読込→計算→保存→一覧まで通ったが、実ファイルは未使用。
  実際のインボイス形式（ヘッダー位置・列位置）が想定と違う可能性あり
- **y84_black/y84_blue の発注済みが0件**になっている。会話中に消し込み or 削除された可能性。
  実態と合っているか要確認（合っていなければ発注済みリストに再登録が必要）

## 未解決の問題

- **RMSキー有効期限切れ**: 2026-07-22で切れている可能性あり（要確認・再発行）
- **main.pyのコメント文字化け**: 一部mojibakeが残っている（動作に影響なし）
- **y76 SKU名不一致**: RakutenProduct="glay"、products="gray"。現在影響なし
- **Amazon Ads API**: 審査中（2026-06-07申請）
- **`tariff_rate_str` が "FREE" → "REE" になる**: `_parse_permit_columns` の正規表現が
  先頭のFを食っている。税率0%の判定自体は正しく動くので実害は表示のみ（未修正）
- **warm-cache cron の遅延**: GitHub Actions の仕様で最大3時間44分の空白が実測されている。
  在庫は5分TTLにしたので画面表示には影響しないが、売上キャッシュ切れ時は7分待ちが発生する

## 次回やること

1. **本番で在庫が自動更新されるか確認**（上記「途中の作業」の期待値と一致するか）
2. **Amazon仕入管理を実インボイス＋輸入許可書で通す**（読込→計算→保存→商品マスタのcost_jpy確認）
3. y84の発注済み0件が実態と合っているか確認
4. Amazonの推奨判定を楽天式（在庫日数 ≤ threshold_days）に揃えるか検討（現状は「発注数 > 0」）
5. `DELIVERY_MATCH_SINCE`（現在2026-07-27）を運用に合わせて見直すか検討
6. タオタロウExcel取込の本番テスト（入庫済みExcelをアップロードして照合確認）
7. 照合ロジックの改善（buy_url以外にも色・サイズで補完照合）
8. RMSキーの有効期限確認・再発行
9. 損益計算・ダッシュボード機能の実装

## Amazon Ads 広告管理（Phase 0+1+2 完了）

- **Phase 0**: API疎通・DB保存 — SP v3 campaigns/adGroups/keywords/targets + 3レポート取得、6テーブルへupsert
- **Phase 1**: 読み取り専用ダッシュボード — キャンペーン/KWパフォーマンス/検索語句の3タブ、フィルタ・ソート・ACOS色分け
- **Phase 2**: 提案一覧 + 結果パネル — KW昇格・入札調整・予算調整の提案表示
- **注意**: 入札変更、KW追加、停止、除外KW登録などのAPI更新系操作は未実装（意図的）
- Profile ID: `879360668189955` (JP)、Base URL: `https://advertising-api-fe.amazon.com`

## 重要ルール
- **git push はユーザーが手動実行**（PowerShell形式で提示）
- **本番DB変更前に対象レコードの全フィールドを取得・保持**
- **execute前に必ずpreviewでpush_valueを確認**
- **set_components = 売れる数を決める部品**（受注時に構成品在庫を増減、RMS在庫再計算に使用）
- **purchase_components = 仕入れ時に必要な部品**（RMS在庫連動しない、TAO太郎Excel展開とCSVのみ）
- **main.pyの在庫連動ロジックはpurchase_componentsを絶対に参照しない**

### DBカラム追加の手順（2026-08-02に本番を止めた事故の教訓）
- **カラム追加は必ず `backend/app/main.py` の `_migrate()` に登録する**。
  ここが起動時に自動実行される唯一の仕組み。
- `migrate_xxx.py` のような独立スクリプトを作っても**本番では実行されない**
  （ローカルの `.env` を見て SQLite に対して走るだけ）。
  モデルに列を足して `_migrate()` に登録し忘れると、本番で該当テーブルのAPIが
  500エラーになる（実際に `/products/` と `/invoices/` が停止した）。
- モデルに列を追加したら、`_migrate()` の migrations 配列にも同じ列を必ず足すこと。

### Amazonの原価・単価の使い分け
- **`products.price` = 仕入れ単価（元）**。ここに円を入れると発注管理の「単価(元)」が壊れる
- **`products.cost_jpy` = 1個あたり原価（円）**。送料・輸入税込み。仕入管理が更新する
- 楽天（`rakuten_products`）も同じ使い分け

### SP-APIの注意点
- **`reservedQuantity` は合計で使わない**。`pendingTransshipment`（FC間移動中）は
  すでにFBAにある在庫なので販売可能側に加算する。合計を「作業中」扱いすると
  在庫があるのに発注推奨が出る
- **v0 `getShipments` は `ShipmentStatusList` を複数まとめて渡すと1件しか返さない**。
  ステータスを1つずつ指定して結合すること
- **在庫と売上でキャッシュTTLを分ける**。在庫は1req・2.3秒、売上は220req・約7分。
  同じTTLにすると在庫まで数時間古くなる

## 異常時ロールバック

1. Render環境変数: RMS_PUSH_ENABLED=false に変更
2. Render再起動
3. 既存.NETツール再開

## 監視項目（5点）

1. inventory-events にpush失敗がないこと（GET /api/inventory-events で確認）
2. y115_10 previewがdiff=0を維持すること
3. is_component=true + rakuten_item_urlありのSKU（16件）受注時にcomponent自体もRMS反映されること
4. 既存ツールは再開しない
5. HTTP 429（レート制限）の再発監視

## スケジューラ構成（main.py）
- 起動時: `_seed_processed_orders()` → `_pull_rms_stock()`
- 1分ごと: `_sync_rakuten_stock()` → `_pull_rms_stock()`
- 30分ごと: `_check_delayed_cancellations()`
- 60分ごと: `_sync_rakuten_sales()`
