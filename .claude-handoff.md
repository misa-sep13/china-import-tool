# 引継ぎメモ

## 今日やったこと

1. **発注済みリスト削除後の再出現バグ修正**
   - `invalidateQueries` → `refetchQueries` に変更して削除後即時反映

2. **削除ボタンラベル変更**
   - 「納品済」→「削除」に変更（誤発注削除対応）

3. **1分ごとRMS在庫同期（受注差分方式）**
   - 全商品一括取得からメモリ節約のため直近2分の受注のみ取得して在庫を差分減算に変更
   - `fetch_recent_orders` 関数を `rakuten_rms.py` に追加

4. **在庫・損益画面に親子階層表示**
   - 商品マスタと同じ `└` インデント表示を実装
   - `/rakuten/stock` に `set_components`・`supplier` を追加

5. **在庫編集保存時にRMSへ即時反映**
   - `update_product` をasync化、stock変更時に `push_inventory_to_rms` を呼び出し

6. **単品在庫変更時にセット商品の在庫を自動計算してRMS反映**
   - 例: 243（1本）=519 → 239（2本セット）=259、241（3本セット）=173 を自動計算・反映
   - `is_component` 問わず対応

7. **RMSから商品管理番号・SKU番号を一括同期**
   - `/rms/sync-sku-mapping` エンドポイントを追加
   - 商品マスタ画面に「🔗 管理番号同期(RMS)」ボタンを追加
   - `rakuten_item_url`（商品管理番号）と `rakuten_sku_id` を自動設定

8. **商品マスタ編集画面に楽天商品管理番号入力欄を追加**
   - 「楽天管理情報」セクションに `rakuten_item_url` の手動入力欄を追加

9. **RMS在庫反映のQPS制限対策**
   - リクエスト間に0.5秒ウェイト・429時に指数バックオフリトライ（最大3回）

10. **y76 2組セット25件をDBに登録**
    - y76_b-b〜y76_w-w の25件、仕入原価180円、販売価格1000円

11. **SKU 243・244の spec 入れ替え**
    - 243=ソフトミント、244=シトラスミント に修正

12. **239・240・241・242の set_components 修正**
    - 正しい参照先に修正（243=ソフトミント系、244=シトラスミント系）

## 途中の作業
- なし（本日の変更はすべてコミット・push済み）

## 未解決の問題
- Renderのメモリ不足（Starter 512MB）は受注差分方式に変更して改善済み。様子見
- y125・y134系のbuy_urlが空のまま（手動で再入力が必要）
- Supabaseのパスワードがセッション会話に過去露出 → ローテーション推奨

## 次回やること
1. RMS在庫反映の動作確認（セット商品が正しく連動しているか継続監視）
2. y125・y134系のbuy_url・仕様・単価を手動で再入力
3. Supabaseパスワードのローテーション
4. **あざみ（就労支援）在庫管理の実装**（次の大きな機能）

## 現在の状態
- 本番API: https://china-import-tool.onrender.com/api
- 本番フロント: https://misa-sep13.github.io/china-import-tool/
- GitHubリポジトリ: https://github.com/misa-sep13/china-import-tool

## 重要メモ
- **RMS在庫反映の仕組み**:
  - 在庫・損益画面で「編集」→在庫変更→「保存」でDB+RMSに即時反映
  - 単品（例:243）を変更するとセット商品（239,241）の在庫も自動計算して反映
  - `rakuten_item_url` にRMSの商品管理番号（例: s08）が必要。「管理番号同期(RMS)」ボタンで一括設定可能
- **1分ごとの受注差分同期**: 売れた分だけ在庫を減算（RMSからのフル取得はしない）
- **PUTエンドポイントは部分更新**（exclude_unset=True）→ 送らなかったフィールドは上書きされない
- 複数のClaudeが同時に本番DBを操作しないこと
- Supabaseパスワードが会話に露出済み → できるだけ早くローテーションを
