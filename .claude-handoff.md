# 引継ぎメモ

## 今日やったこと（2026-06-22〜23）

### 1. モデル切り替え
- claude-sonnet-4-6 → Fable 5 を試みるも未提供 → **claude-opus-4-8** に変更（CLAUDE.md更新済み）

### 2. 内部SKU（構成品）設定商品の在庫が0になる不具合を修正【push済み】
- 原因: 在庫取得処理が `is_component != True` で内部SKUをクエリ除外し、さらに構成品⇔セット間の在庫計算（Step2/Step3）がRMSの正値を `min(0,...)=0` で潰していた
- 修正方針（ユーザー確認済みの仕様）:
  - **セット販売SKU（is_component=false）= RMSが在庫の基盤**。RMSから取れた値をそのまま使う
  - **内部SKU（is_component=true、構成品）= RMSに在庫が無い**（発注→入荷処理でDB在庫が入る）。RMS問い合わせには送らず、DB在庫を維持
  - **構成品⇔セット間の在庫計算（Step2/Step3）を廃止**（RMSの正値を潰すため有害）
- 変更ファイル: `backend/app/api/routes/rakuten.py`（import_stock_from_rms）、`backend/app/main.py`（_pull_rms_stock）

### 3. RMS在庫取得を1分ごと＋起動時に実行【push済み】
- スケジューラ `_scheduler_loop` を変更: RMS在庫取得を60分→**1分ごと**、さらに**起動時に1回**実行
- 販売数同期（重い）は従来通り60分ごと据え置き
- 目的: 発注管理・在庫損益ページを開けばほぼ最新（最大1分前）の在庫が出る。手動「在庫取得(RMS)」ボタンを押す必要をなくす
- 変更ファイル: `backend/app/main.py`

### 4. 在庫保存の高速化【push済み】
- 原因: 保存時にRMSへのPUT完了を `await` で待っていたため1件35秒かかっていた
- 修正: `bulk-update-stock` と `PUT /products/{id}` のRMS反映を `BackgroundTasks` に切り出し、保存レスポンスを即返す。RMS反映自体は裏で継続。輸送中(inbound)のみ更新時はpush対象外
- 変更ファイル: `backend/app/api/routes/rakuten.py`

### 5. 在庫入力欄の「0残り」修正【push済み・デプロイ確認済み】
- 実在庫・輸送中・規定在庫の入力欄に `onFocus={selectIfZero}` を追加。フォーカス時に値が0なら全選択し、数字を打てば上書き（030にならない）
- 変更ファイル: `frontend/src/pages/RakutenStockPage.jsx`
- 本番JS（index-DKhcUZ0W.js）に反映済みを確認。ユーザー側はCtrl+F5でキャッシュクリアが必要な場合あり

### 6. 別ツールの発注を「輸送中(inbound)」として登録（画面手入力）
- 別の発注管理ツールで発注済みの商品を、こちらの「在庫・損益一覧」で輸送中に手入力する作業を開始
- **重要な仕様**: セット商品は枚数→セット数に換算が必要（商品ごとに違う）。例: y27=ダブルガーゼ12枚セットは「2200枚÷12=183セット」
- 発注推奨数の計算（rakuten_calc.py）は `total_stock = stock + inbound + ordered` で、輸送中が増えると提案発注数が減る・発注タイミングからも外れることを確認済み

## 途中の作業（次回ここから再開）

### 輸送中(inbound)の手入力作業（継続中）
別ツール（画像で確認）の発注数を、こちらの「在庫・損益一覧」ページで輸送中に入力中。SKU照合済みの登録予定リスト:

| SKU | 輸送中に入れる数 | 状態 |
|---|---|---|
| y27 | **183** | ⚠️ 誤って180で保存済み → **180→183に要修正** |
| y104_gold | 30 | 未確認（y104→y104_goldに変更指示あり） |
| y125 | 50 | 未確認 |
| y58_navy | 20 | 未確認 |
| y58_black | 30 | 未確認 |
| y71_black | 10 | 未確認 |
| y71_darkglay | 30 | 未確認 |
| y71_lightglay | 15 | 未確認 |

- **y27は本番で輸送中=180になっている（183が正しい）。次回まず修正すること**
- y91系（y91_black/blue/clear/pink）は別ツールで発注あり（y91_クリア未/y91_ブラック60など画像参照）だが輸送中はまだ未入力
- 入力は「在庫・損益一覧」ページ（https://misa-sep13.github.io/china-import-tool/?v=1#/rakuten/stock ）で検索→輸送中列に入力→一括保存

## 未解決の問題

### RMS在庫取得で not_found だった11件（要調査）
手動 import-stock 実行時、RMSから在庫が取れなかったSKU（在庫を取りたいなら原因調査が必要）:
- `y83_pink` `y83_darkgray` `y83_black` `y83_beige` `y83_lightgray` `y83_gray`（y83系6色）
- `s02-1` `s02-1-banana` `s02-1-grape` `s02-1-lemontea` `s02-1-peach`（チェックアップジェル単品系5件）
- 原因候補: RMS未登録、またはmanage_number/variant_idの不一致

### y91在庫0問題は解決済み
- y91の実在庫0は「デプロイ直後でスケジューラのRMS取得が未実行だった」だけ。手動import-stockで正しく反映（y91_black=17, blue=208, clear=77, pink=90、RMS管理画面と一致）。コードのバグではなかった

## 次回やること（優先度順）

1. **y27の輸送中を180→183に修正**（最優先・誤入力の訂正）
2. 輸送中の手入力続き（上記テーブルのy104_gold〜y71系、y91系）
3. not_found 11件（y83系・s02-1系）のRMS在庫取得失敗の原因調査
4. **🔴 RMSキー再発行**（前回からの継続・優先度高）— APIレスポンスにキー露出したため。再発行後Supabaseの rakuten_settings 更新
5. 在庫連動（push_inventory_to_rms）の本格運用切り替え（計算値が一致したら）
6. y125・y134系のbuy_url・仕様・単価の手動再入力（前回から継続）

## 現在の状態

- リポジトリ: https://github.com/misa-sep13/china-import-tool
- 本番API: https://china-import-tool.onrender.com/api
- 本番フロント: https://misa-sep13.github.io/china-import-tool/
- ローカル起動: `cd C:\Users\misa\Projects\china-import-tool\backend; python -m uvicorn app.main:app --port 8000`
- フロントビルド: `cd C:\Users\misa\Projects\china-import-tool\frontend; npm run build`
- 全コミットpush済み（最新: 7e30da6 0残り修正）
- 未追跡ファイル: `backend/migrate_add_show_in_orders.py`（前回から「不要なら削除可」のまま）

## 重要メモ

- **git push はユーザーが手動実行**（Claude Code の auto-mode でブロックされるため）
- **複数のClaudeが同時に本番DBを操作しないこと**
- **在庫の単位は商品マスタのSKU単位**（例: y27は「12枚セット」が1個）。別ツールの発注枚数→セット数への換算は商品ごとに違うので一件ずつ確認する
- **inbound（輸送中）はRMSに反映しない**（発注中の数なので楽天販売在庫と無関係）。**stock（実在庫）はRMSに反映する**
- 発注推奨数: `total_stock = stock + inbound + ordered`、`order_qty = 予測販売数 + リードタイム販売 + 安全在庫 - total_stock`（rakuten_calc.py）
- RMS在庫取得の仕様: manage_number（rakuten_item_url優先、無ければskuの"_"前）＋ variant_id（=sku）でbulk-get問い合わせ
- 本番DBはWebFetchだと全件取れず一部SKUが欠落する。確認は `Invoke-RestMethod` で生レスポンスを取ること（WebFetchは信頼しない）
