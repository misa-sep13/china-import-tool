# 引継ぎメモ

## 今日やったこと

1. **在庫差分同期のバグ修正（根本原因を特定・修正）**
   - `getOrder` APIに `version: 10` パラメータが必要だった（なかったため注文詳細が取れていなかった）
   - SKUの取得先を `manageNumber` → `SkuModelList[].variantId` に変更（`y49_pink2` 形式のSKUが取れるようになった）
   - Renderサーバーの時刻がUTCのため `datetime.now()` がJSTとズレていた → `datetime.now(timezone.utc).astimezone(JST)` に修正

2. **在庫一覧の検索を全角対応**
   - 「ｙ49」（全角）で検索しても「y49」がヒットするよう正規化

3. **在庫同期にセット商品の逆方向計算を追加**
   - セット商品が売れたとき → 構成品の在庫も減算
   - 単品が売れたとき → その単品を使う全セット商品の在庫を再計算
   - y76（2組セット）のような「単品同士の組み合わせセット」にも対応

4. **在庫同期ログページを追加**
   - アプリのサイドバーに「🔍 同期ログ」ページを追加
   - 注文が入るたびに「売れた商品」と「RMS反映予定値（確認中）」を表示
   - 30秒自動更新
   - APIエンドポイント: `GET /api/sync-logs`

## 途中の作業
- **在庫連動の切り替え検証中**
  - 現在：RMSへの反映はログに出すだけ（`rms_would_update`）で実際には送っていない
  - 別の在庫管理ツールがまだRMSを管理している
  - 同期ログページでこのツールの計算値と別ツールの値が一致することを確認してから切り替える

## 未解決の問題
- **別の在庫管理ツールとの切り替えが未完了**
  - 同期ログで値が一致したら `_sync_rakuten_stock` の `rms_would_update` を実際に `push_inventory_to_rms` に渡す実装が必要
  - その後、別ツールを停止する
- **Supabaseパスワードが過去セッションで露出済み** → ローテーション推奨
- y125・y134系のbuy_urlが空のまま（手動で再入力が必要）

## 次回やること
1. **同期ログページを確認して計算値が正しいか検証**（注文が来るたびに確認）
2. 別ツールの計算値と一致したら `push_inventory_to_rms` を呼び出すコードを追加して切り替え
3. y125・y134系のbuy_url・仕様・単価を手動で再入力
4. Supabaseパスワードのローテーション

## 現在の状態
- 本番API: https://china-import-tool.onrender.com/api
- 本番フロント: https://misa-sep13.github.io/china-import-tool/
- GitHubリポジトリ: https://github.com/misa-sep13/china-import-tool
- ローカル起動: `cd C:\Users\misa\Projects\china-import-tool\backend; python -m uvicorn app.main:app --port 8000`

## 重要メモ
- **在庫連動の現状フロー**:
  1. 楽天で注文 → RMSが自動で在庫減算（楽天側）
  2. 1分後にこのツールが受注取得 → DBの在庫を減算・セット再計算
  3. RMSへの反映はまだしていない（ログのみ）
  4. 別の在庫管理ツールが引き続きRMSを管理中
- **切り替え手順**:
  - `main.py` の `_sync_rakuten_stock` で `rms_would_update` を計算した後に `push_inventory_to_rms` を呼ぶだけ
  - 切り替え前に「在庫取得(RMS)」ボタンでDBをRMSに合わせておくこと
- **複数のClaudeが同時に本番DBを操作しないこと**
- `r-sku00000015` というSKUが受注データに出てきたがDBに未登録（50件未登録の中の一つ）
